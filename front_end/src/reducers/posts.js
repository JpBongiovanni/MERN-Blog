const reducer = (state, action) => {
    if(action.type === 'CREATE'){
        switech (action.type){
            case 'FETCH_ALL':
                return state;
            case 'FETCH_ALL':
                return state;
            case 'FETCH_ALL':
                return state;
        }
    }
}